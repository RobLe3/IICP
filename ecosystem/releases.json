{
  "schema_version": 1,
  "generated_at": "2026-07-31",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.1",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No wire or runtime migration is required.",
      "rollback": "Continue using the immutable v1.10.0 normative archive; only structured ecosystem metadata is corrected."
    },
    {
      "id": "directory-php",
      "version": "1.10.83",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "replica-lifecycle-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No schema migration is required; active replicas may adopt authenticated decommissioning and same-DID reactivation.",
      "rollback": "Restore PHP directory v1.10.82; replicas should re-register to rotate credentials before resuming protected snapshot access."
    },
    {
      "id": "directory-rust",
      "version": "0.1.5",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "replica-lifecycle-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No schema migration is required; configure a stable replica DID and HTTPS endpoint before replica-mode use.",
      "rollback": "Stop the preview and restore v0.1.4; re-register before protected snapshot bootstrap. Do not treat rollback as Genesis cutover authority."
    },
    {
      "id": "client-python",
      "version": "0.7.99",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No runtime or wire migration is required.",
      "rollback": "Install the previous immutable package version; runtime behavior and persisted node state are unchanged."
    },
    {
      "id": "client-typescript",
      "version": "0.7.99",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No runtime or wire migration is required.",
      "rollback": "Install the previous immutable package version; runtime behavior and persisted node state are unchanged."
    },
    {
      "id": "client-rust",
      "version": "0.7.99",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No runtime or wire migration is required.",
      "rollback": "Install the previous immutable crate version; this restores its earlier dependency graph without changing persisted node state."
    },
    {
      "id": "web-node",
      "version": "0.2.2",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild the browser package and preserve existing encrypted provider state.",
      "rollback": "Redeploy the previous immutable browser package; do not enable plaintext fallback."
    }
  ]
}
