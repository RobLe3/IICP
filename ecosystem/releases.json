{
  "schema_version": 1,
  "generated_at": "2026-08-02",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.4",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "mcp-era-negotiation-v0",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "Providers may add separate implementation identity/version and SDK compatibility fields; sdk_version remains a compatibility alias.",
      "rollback": "Continue using the immutable v1.10.3 archive; legacy sdk_version-only registrations remain valid."
    },
    {
      "id": "directory-php",
      "version": "1.10.86",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Run the reversible node implementation metadata migration before serving the release.",
      "rollback": "Roll back the application, then drop the three additive metadata columns after confirming no newer provider registrations depend on them."
    },
    {
      "id": "directory-rust",
      "version": "0.1.8",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Apply migration 020 to an existing preview schema; empty preview databases use the updated baseline.",
      "rollback": "Restore v0.1.7 and drop the three additive metadata columns; Genesis cutover remains unauthorized."
    },
    {
      "id": "client-python",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-typescript",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-rust",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "web-node",
      "version": "0.2.2",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild the browser package and preserve existing encrypted provider state.",
      "rollback": "Redeploy the previous immutable browser package; do not enable plaintext fallback."
    }
  ]
}
