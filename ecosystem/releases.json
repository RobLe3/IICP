{
  "schema_version": 1,
  "generated_at": "2026-08-13",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.13",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-public-v1",
        "directory-dispatch-v1",
        "directory-lifecycle-v1",
        "service-lifecycle-v1",
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No base-wire, directory-schema or persisted-state migration is required. The negotiated service-lifecycle profile now has a defined native lifecycle envelope; Registry 1.4 validation is stricter while preserving existing intent URNs and legacy payload_schema fields.",
      "rollback": "Continue using immutable v1.10.11 artifacts; implementations must not claim the corrected native lifecycle-envelope mapping or strict Registry 1.4 evidence validation."
    },
    {
      "id": "directory-php",
      "version": "1.10.90",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No database migration is required. The public 24-hour stats aggregate now selects only the newest deterministic row per metric in SQL.",
      "rollback": "Restore the immutable 1.10.89 artifact; the public stats cache-miss path then returns to unbounded historical-row hydration and may exceed shared-host PHP memory limits."
    },
    {
      "id": "directory-rust",
      "version": "0.1.11",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1",
        "runtime-health-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No database migration is required. This operator-preview release adds local runtime-health and opt-in systemd service support, a checksum-bound staged updater, deterministic bounded stats parity with PHP v1.10.90, and crates.io distribution.",
      "rollback": "Restore v0.1.10 with the guarded updater. PHP remains Genesis authority; rollback does not require a database migration or authorize federation or cutover."
    },
    {
      "id": "client-python",
      "version": "0.7.103",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "service-lifecycle-v1",
        "runtime-health-v1",
        "managed-operator-v1",
        "cip-consumer-cosignature-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No persisted-state or wire migration is required. This release publishes the opt-in eligibility-bounded candidate-ranker seam, shared heterogeneous-routing evidence, and IICP-E050 credential-lifecycle replay already implemented across the official SDKs. Built-in selection remains the fallback, and IICP retains final policy validation and dispatch authority.",
      "rollback": "Install the preceding immutable 0.7.102 package. Existing wire behavior remains compatible; the local candidate-ranker extension and the new credential-lifecycle regression evidence are then unavailable."
    },
    {
      "id": "client-typescript",
      "version": "0.7.103",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "service-lifecycle-v1",
        "runtime-health-v1",
        "managed-operator-v1",
        "cip-consumer-cosignature-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No persisted-state or wire migration is required. This release publishes the opt-in eligibility-bounded candidate-ranker seam, shared heterogeneous-routing evidence, and IICP-E050 credential-lifecycle replay already implemented across the official SDKs. Built-in selection remains the fallback, and IICP retains final policy validation and dispatch authority.",
      "rollback": "Install the preceding immutable 0.7.102 package. Existing wire behavior remains compatible; the local candidate-ranker extension and the new credential-lifecycle regression evidence are then unavailable."
    },
    {
      "id": "client-rust",
      "version": "0.7.103",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [
        "service-lifecycle-v1",
        "runtime-health-v1",
        "managed-operator-v1",
        "cip-consumer-cosignature-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No persisted-state or wire migration is required. This Rust-only security patch replaces the external kill(1)-based single-instance check with a direct OS liveness check, fails closed on indeterminate lock state, and preserves the existing node PID-file location.",
      "rollback": "Install the preceding immutable 0.7.102 package only if the environment provides a reliable external kill executable. Rollback restores the documented minimal-container duplicate-node risk and is not recommended."
    },
    {
      "id": "web-node",
      "version": "0.2.3",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild or install the browser package; existing encrypted provider state remains compatible.",
      "rollback": "Restore v0.2.2; older directories continue to consume the legacy sdk_version compatibility alias."
    }
  ]
}
