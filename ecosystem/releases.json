{
  "schema_version": 1,
  "generated_at": "2026-08-14",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.13",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-public-v1",
        "directory-dispatch-v1",
        "directory-lifecycle-v1",
        "service-lifecycle-v1",
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No base-wire, directory-schema or persisted-state migration is required. The negotiated service-lifecycle profile now has a defined native lifecycle envelope; Registry 1.4 validation is stricter while preserving existing intent URNs and legacy payload_schema fields.",
      "rollback": "Continue using immutable v1.10.11 artifacts; implementations must not claim the corrected native lifecycle-envelope mapping or strict Registry 1.4 evidence validation."
    },
    {
      "id": "directory-php",
      "version": "1.10.91",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1",
        "effective-capability-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Run the additive capability version/phase migration. The directory preserves, projects and matches explicit effective-capability variants while retaining legacy capability fields and default discovery behavior.",
      "rollback": "Restore immutable 1.10.90 application code. The expand-first capability columns may remain unused; do not destructively roll back the schema. Genesis deployment requires a separate operator decision."
    },
    {
      "id": "directory-rust",
      "version": "0.1.12",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1",
        "runtime-health-v1",
        "effective-capability-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Run the additive effective-capability migration. This operator-preview release aligns capability persistence/matching and directory lifecycle transitions with the shared PHP contracts.",
      "rollback": "Restore immutable 0.1.11 through the guarded updater. The expand-first capability columns may remain unused; PHP remains Genesis authority and no cutover is authorized."
    },
    {
      "id": "client-python",
      "version": "0.7.104",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "service-lifecycle-v1",
        "runtime-health-v1",
        "managed-operator-v1",
        "cip-consumer-cosignature-v1",
        "effective-capability-v1",
        "runtime-identity-context-v0",
        "dispatch-route-ticket-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No persisted-state or base-wire migration is required. This release adds opt-in runtime identity composition, explicit effective-capability variants, and fail-closed proxy route-ticket verification. Existing behavior is preserved when the new local context is disabled and legacy discovery remains an explicit rollback mode.",
      "rollback": "Install immutable 0.7.103. This removes the new runtime-identity and effective-capability interfaces and restores the previous proxy ticket behavior; do not use the rollback where verified ticketed discovery is required."
    },
    {
      "id": "client-typescript",
      "version": "0.7.104",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "service-lifecycle-v1",
        "runtime-health-v1",
        "managed-operator-v1",
        "cip-consumer-cosignature-v1",
        "effective-capability-v1",
        "runtime-identity-context-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No persisted-state or base-wire migration is required. This release adds opt-in runtime identity composition and explicit effective-capability variants. Existing requests remain unchanged when runtime identity is disabled.",
      "rollback": "Install immutable 0.7.103. Existing wire behavior remains compatible; the runtime-identity and effective-capability interfaces are then unavailable."
    },
    {
      "id": "client-rust",
      "version": "0.7.104",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "service-lifecycle-v1",
        "runtime-health-v1",
        "managed-operator-v1",
        "cip-consumer-cosignature-v1",
        "effective-capability-v1",
        "runtime-identity-context-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No persisted-state or base-wire migration is required. This release adds opt-in runtime identity composition and explicit effective-capability variants while retaining the 0.7.103 portable single-instance security fix.",
      "rollback": "Install immutable 0.7.103. Existing wire behavior and the single-instance fix remain available; the runtime-identity and effective-capability interfaces are then unavailable."
    },
    {
      "id": "web-node",
      "version": "0.2.4",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "dispatch-route-ticket-v1",
        "effective-capability-v1",
        "runtime-identity-context-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild or install the browser package. The release adds fail-closed route-ticket verification, opt-in runtime identity composition and explicit effective-capability variants; existing encrypted provider state remains compatible.",
      "rollback": "Restore immutable 0.2.3. This removes the new parity interfaces and browser ticket verification; do not use the rollback where verified ticketed discovery is required."
    }
  ]
}
