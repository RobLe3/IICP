{
  "schema_version": 1,
  "generated_at": "2026-08-01",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.3",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No wire or runtime migration is required; the modern MCP era remains explicit and the discovery evidence profile remains additive.",
      "rollback": "Continue using the immutable v1.10.2 archive; gateways retain the legacy MCP default and implementations may ignore additive evidence metadata."
    },
    {
      "id": "directory-php",
      "version": "1.10.85",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No database migration is required; SDK release-currency evidence advances to 0.7.101.",
      "rollback": "Restore the immutable v1.10.84 release; routing and compatibility behavior are unchanged."
    },
    {
      "id": "directory-rust",
      "version": "0.1.7",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No database migration is required; SDK release-currency evidence advances to 0.7.101.",
      "rollback": "Restore the immutable v0.1.6 preview; no Genesis or schema change is involved."
    },
    {
      "id": "client-python",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-typescript",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-rust",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "web-node",
      "version": "0.2.2",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild the browser package and preserve existing encrypted provider state.",
      "rollback": "Redeploy the previous immutable browser package; do not enable plaintext fallback."
    }
  ]
}
