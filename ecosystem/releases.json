{
  "schema_version": 1,
  "generated_at": "2026-08-01",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.2",
      "wire_behavior": "additive",
      "conformance_impact": "unchanged",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No wire or runtime migration is required; the discovery-evidence profile remains additive and pre-normative.",
      "rollback": "Continue using the immutable v1.10.1 archive; implementations may ignore the additive evidence profile."
    },
    {
      "id": "directory-php",
      "version": "1.10.84",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No schema migration is required; discovery responses add content-free explanatory evidence.",
      "rollback": "Restore the preceding immutable release; older clients and directories may omit the additive evidence fields."
    },
    {
      "id": "directory-rust",
      "version": "0.1.6",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No schema migration is required; discovery responses add content-free explanatory evidence.",
      "rollback": "Restore the preceding immutable release; older clients and directories may omit the additive evidence fields."
    },
    {
      "id": "client-python",
      "version": "0.7.100",
      "wire_behavior": "additive",
      "conformance_impact": "unchanged",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; clients tolerate directories that omit the additive evidence fields.",
      "rollback": "Install the preceding immutable package; discovery remains compatible and additive fields are no longer exposed by the client model."
    },
    {
      "id": "client-typescript",
      "version": "0.7.100",
      "wire_behavior": "additive",
      "conformance_impact": "unchanged",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; clients tolerate directories that omit the additive evidence fields.",
      "rollback": "Install the preceding immutable package; discovery remains compatible and additive fields are no longer exposed by the client model."
    },
    {
      "id": "client-rust",
      "version": "0.7.100",
      "wire_behavior": "additive",
      "conformance_impact": "unchanged",
      "profiles": [
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; clients tolerate directories that omit the additive evidence fields.",
      "rollback": "Install the preceding immutable package; discovery remains compatible and additive fields are no longer exposed by the client model."
    },
    {
      "id": "web-node",
      "version": "0.2.2",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild the browser package and preserve existing encrypted provider state.",
      "rollback": "Redeploy the previous immutable browser package; do not enable plaintext fallback."
    }
  ]
}
