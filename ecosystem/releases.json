{
  "schema_version": 1,
  "generated_at": "2026-08-08",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.10",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "directory-public-v1",
        "directory-dispatch-v1",
        "directory-lifecycle-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No base-wire, directory-schema or persisted-state migration is required. The negotiated lifecycle profile gains explicit incremental streaming sequences, while existing CALL/RESPONSE and HTTP/CBOR behavior remains buffered.",
      "rollback": "Continue using v1.10.9 artifacts; negotiated lifecycle streaming implementations must not claim the v1.1.0 draft fixture."
    },
    {
      "id": "directory-php",
      "version": "1.10.88",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "required",
      "migration": "No database migration is required; install the immutable 1.10.88 artifact and retain the existing environment and schema.",
      "rollback": "Restore 1.10.87 only as an emergency runtime rollback; its CommonMark dependency contains the reviewed advisories and should not remain deployed."
    },
    {
      "id": "directory-rust",
      "version": "0.1.8",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Apply migration 020 to an existing preview schema; empty preview databases use the updated baseline.",
      "rollback": "Restore v0.1.7 and drop the three additive metadata columns; Genesis cutover remains unauthorized."
    },
    {
      "id": "client-python",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-typescript",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-rust",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "web-node",
      "version": "0.2.3",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild or install the browser package; existing encrypted provider state remains compatible.",
      "rollback": "Restore v0.2.2; older directories continue to consume the legacy sdk_version compatibility alias."
    }
  ]
}
