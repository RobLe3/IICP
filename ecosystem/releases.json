{
  "schema_version": 1,
  "generated_at": "2026-07-31",
  "releases": [
    {
      "id": "specification",
      "version": "1.9.3",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "none",
      "rollback": "Continue using the previous immutable specification release."
    },
    {
      "id": "directory-php",
      "version": "1.10.82",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "deployment-provenance-v1",
        "replica-snapshot-auth-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No schema migration; configure signed deployment metadata and rotate newly issued replica tokens to snapshot scope.",
      "rollback": "Restore the previous immutable release and its configuration; the former replica token scope remains accepted for one compatibility window."
    },
    {
      "id": "directory-rust",
      "version": "0.1.4",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "replica-snapshot-auth-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No schema migration; upgrade before any authenticated Genesis snapshot preflight.",
      "rollback": "Stop the replica before restoring v0.1.3; v0.1.3 must not perform an authenticated production snapshot preflight."
    },
    {
      "id": "client-python",
      "version": "0.7.99",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No runtime or wire migration is required.",
      "rollback": "Install the previous immutable package version; runtime behavior and persisted node state are unchanged."
    },
    {
      "id": "client-typescript",
      "version": "0.7.99",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No runtime or wire migration is required.",
      "rollback": "Install the previous immutable package version; runtime behavior and persisted node state are unchanged."
    },
    {
      "id": "client-rust",
      "version": "0.7.99",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No runtime or wire migration is required.",
      "rollback": "Install the previous immutable crate version; this restores its earlier dependency graph without changing persisted node state."
    },
    {
      "id": "web-node",
      "version": "0.2.2",
      "wire_behavior": "unchanged",
      "conformance_impact": "unchanged",
      "profiles": [],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild the browser package and preserve existing encrypted provider state.",
      "rollback": "Redeploy the previous immutable browser package; do not enable plaintext fallback."
    }
  ]
}
