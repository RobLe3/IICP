{
  "schema_version": 1,
  "generated_at": "2026-08-09",
  "releases": [
    {
      "id": "specification",
      "version": "1.10.12",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-public-v1",
        "directory-dispatch-v1",
        "directory-lifecycle-v1",
        "service-lifecycle-v1",
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No base-wire, directory-schema or persisted-state migration is required. The negotiated service-lifecycle profile now has a defined native lifecycle envelope; Registry 1.4 validation is stricter while preserving existing intent URNs and legacy payload_schema fields.",
      "rollback": "Continue using immutable v1.10.11 artifacts; implementations must not claim the corrected native lifecycle-envelope mapping or strict Registry 1.4 evidence validation."
    },
    {
      "id": "directory-php",
      "version": "1.10.89",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No database migration is required. Reputation score, task counters and the hourly positive-gain budget are now committed in one node-locked transaction.",
      "rollback": "Restore the immutable 1.10.88 artifact; concurrent successful heartbeats can then exceed the intended shared hourly reputation budget, so rollback is for emergency recovery only."
    },
    {
      "id": "directory-rust",
      "version": "0.1.9",
      "wire_behavior": "unchanged",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1",
        "discovery-evidence-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "No database migration is required. Reputation updates now serialize the shared hourly budget and retry bounded transient MySQL lock failures.",
      "rollback": "Restore v0.1.8; Genesis cutover remains unauthorized and the earlier concurrent reputation behavior is not parity-safe."
    },
    {
      "id": "client-python",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-typescript",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "client-rust",
      "version": "0.7.101",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "mcp-era-negotiation-v0"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "optional",
      "migration": "No persisted-state migration is required; MCP 2025-11-25 remains the default and 2026-07-28 is explicit.",
      "rollback": "Install the preceding immutable 0.7.100 package; existing legacy MCP gateway behavior remains available."
    },
    {
      "id": "web-node",
      "version": "0.2.3",
      "wire_behavior": "additive",
      "conformance_impact": "changed",
      "profiles": [
        "directory-implementation-metadata-v1"
      ],
      "supported_protocol": ">=1.9.0,<2.0.0",
      "upgrade": "recommended",
      "migration": "Rebuild or install the browser package; existing encrypted provider state remains compatible.",
      "rollback": "Restore v0.2.2; older directories continue to consume the legacy sdk_version compatibility alias."
    }
  ]
}
